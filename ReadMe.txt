Typing make or make P0 will create the P0 executable. 

make cleanAll, will remove the executable, all object files, and out. files.

make cleanTest1, will remove the P0_test1.traversal files.

make cleanTest2, will remove the P0_test1.traversal files.

make cleanTest3, will remove the P0_test1.traversal files.

The instructions weren't clear on if user only enter the executable on command line. 
If this is the case program will read from keyboard until EOF token is sent.